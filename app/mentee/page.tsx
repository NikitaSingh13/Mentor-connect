import React from 'react'; // Import React library
import Dashboard from './dashboard/page';

// Define and export the default function component named 'page'
export default function page() {
  return (
    <div>
      <Dashboard /> {/* Render the Dashboard component inside a div */}
    </div>
  );
}
